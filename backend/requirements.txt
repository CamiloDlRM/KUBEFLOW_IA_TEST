# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.34.0

# Data validation & settings
pydantic==2.9.2
pydantic-settings==2.6.0

# Database ORM
sqlmodel==0.0.22
psycopg2-binary==2.9.10
alembic==1.13.3

# Auth
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-jose[cryptography]==3.3.0
python-multipart==0.0.20

# Task Queue
celery==5.4.0
redis==5.2.1

# HTTP client
httpx==0.28.0

# Object storage (MinIO / S3) — datasets and the MLflow artifact store
boto3==1.35.76

# DuckDB runs the gold definition: a gold table is one query over the silver
# Parquet files, and running it in-process beats standing up an engine to ask
# one question per extraction. Parquet itself comes from pyarrow, which is
# pinned further down — capped at <15 by mlflow, see the note there.
duckdb==1.1.3

# Email
aiosmtplib==3.0.2

# Notebook execution
papermill==2.6.0
nbformat==5.10.4
nbclient==0.10.1
ipykernel==6.29.5

# ML tracking
mlflow==2.9.2

# Structured logging
structlog==24.4.0

# Metrics exposition for Prometheus
prometheus-client==0.21.1

# AI advisor (Anthropic API)
anthropic>=0.75.0

# ML libraries
scikit-learn==1.5.2
joblib==1.4.2
pandas==2.2.3
numpy==1.26.4

# Dataset readers — needed by pandas for the formats we accept on upload
# (.parquet -> pyarrow, .xlsx -> openpyxl), both for the preview endpoint and
# for notebooks that read DATASET_PATH.
#
# pyarrow also writes and reads every medallion layer, so this pin is now load
# bearing in two places. It is capped at <15 by mlflow 2.9.2, which makes
# 14.0.2 the highest usable version; bump it only together with mlflow, and
# check `core.medallion` still passes afterwards.
pyarrow==14.0.2
openpyxl==3.1.5

pytest>=7.0
pytest-cov>=4.0
pytest-asyncio>=0.23